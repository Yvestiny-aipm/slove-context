# Node 1.2 + 1.3 local backend. Install with venv + pip only (see README).
# Alembic is for reviewable migrations. The FastAPI process does not open a
# DB session in this node. No queue client or model/LLM client.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
jsonschema>=4.18.0
pytest>=7.0.0
ruff>=0.8.0
mypy>=1.13.0
alembic>=1.14.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0

# Node 1.2 local backend. Install with venv + pip only (see README).
# No database driver, queue client, or model/LLM client.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pytest>=7.0.0
ruff>=0.8.0
mypy>=1.13.0

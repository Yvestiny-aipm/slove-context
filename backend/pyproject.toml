[project]
name = "slove-context"
version = "0.0.0"
description = "Local monolith (node 2.3). Health/version, audit logs, Story Project / Spec, Canon, Snapshot APIs."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.27.0",
    "jsonschema>=4.18.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["slove_context*"]

[tool.ruff]
target-version = "py311"
src = ["."]

[tool.mypy]
python_version = "3.11"
pretty = true
